


describe('template spec', () => {  
  it('passes', () => {
    cy.log('hello world')
  })
})


// Usa Mocha para los test, para llamar a una funcion tenemos que poner CY. y ls opciones
// el describe nos indica que es un nuevo test y adentro eta formanado por un string y una funcinon